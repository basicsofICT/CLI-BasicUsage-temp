{
  "name": "Ubuntu 24.04 CLI",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu-24.04",
  "remoteUser": "vscode",
  "updateRemoteUserUID": false,
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y nano tree cowsay && chmod +x .github/classroom/check.sh",
  "customizations": {
    "vscode": {
      "settings": {
        "terminal.integrated.defaultProfile.linux": "bash"
      },
      "extensions": []
    }
  }
}
